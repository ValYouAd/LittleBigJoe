<iframe
        id="{{ video.providerVideoId }}"
        src="//player.vimeo.com/video/{{ video.providerVideoId }}?{{ videoParameters }}"
        width="{{ playerWidth }}"
        height="{{ playerHeight }}"
        frameborder="0">
</iframe>