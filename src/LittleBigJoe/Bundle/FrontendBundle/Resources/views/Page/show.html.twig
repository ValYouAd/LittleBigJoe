{% extends '::base.html.twig' %}

{% block title %}
    {{ entity.title }} | {{ parent() }}
{% endblock %}

{% block body %}
    <section>
        <article>
            <div class="padding10 white-container cmsContent">
                <div class="article-header">
                    <h2><span class="grand-hotel">{{ entity.0.title }}</span></h2>
                </div>

                <div class="nodata">
                    {{ entity.0.content|raw }}
                </div>
            </div>
        </article>
    </section>
{% endblock %}
