<div class="reward-container">
    <div class="title"><span class="orange-text">{{ reward.title }}</span></div>

    <div class="participating">
        <img class="icon" src="{{ asset('static/img/icons/icon-people-gray.png') }}" alt="alt-LBJ - people icon" />
        <span class="orange-text">{{ nb_participants }}</span> {{ 'participant(s)'|trans() }} <span class="light-text">/ {{ reward.stock }} {{ 'max.'|trans() }}</span>
    </div>

    <div class="text-infos">
        <p>{{ reward.description }}</p>
    </div>

    {% if reward.stock is not null %}
        <div class="ribbon"><span>{{ 'Limited'|trans() }}</span></div>
    {% endif %}
</div>