{% extends '::admin.html.twig' %}

{% block title %}
    {{ 'backend.categories_list'|trans() }} | {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-header">
        <h1><span class="glyphicon glyphicon-list"></span> {{ 'backend.categories_list'|trans() }}</h1>
    </div>
    <p class="lead">{{ 'backend.categories_list_resume'|trans() }}</p>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'backend.ID'|trans(), 'c.id') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.name'|trans(), 'c.name') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.slug'|trans(), 'c.slug') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.is_visible'|trans(), 'c.isVisible') }}</th>
            <th>{{ 'backend.actions'|trans() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td>
                    <a href="{{ path('littlebigjoe_backendbundle_categories_show', { 'id': entity.id }) }}">#{{ entity.id }}</a>
                </td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.slug }}</td>
                <td>
                    {% if entity.isVisible %}
                        <span class="label label-success">{{ 'backend.visible'|trans() }}</span>
                    {% else %}
                        <span class="label label-default">{{ 'backend.invisible'|trans() }}</span>
                    {% endif %}
                </td>
                <td>
                    <a class="btn btn-primary btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_categories_show', { 'id': entity.id }) }}">{{ 'backend.show'|trans() }}</a>
                    <a class="btn btn-default btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_categories_edit', { 'id': entity.id }) }}">{{ 'backend.edit'|trans() }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <p>
        <a class="btn btn-primary" href="{{ path('littlebigjoe_backendbundle_categories_new') }}">
            {{ 'backend.create_category'|trans() }}
        </a>
    </p>
{% endblock %}
