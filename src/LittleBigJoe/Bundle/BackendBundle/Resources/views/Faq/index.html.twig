{% extends '::admin.html.twig' %}

{% block title %}
    {{ 'FAQ\'s list'|trans() }} | {{ parent() }}
{% endblock %}

{% block body %}
    <div class="faq-header">
        <h1><span class="glyphicon glyphicon-list"></span> {{ 'FAQ\'s list'|trans() }}</h1>
    </div>
    <p class="lead">{{ 'Below you\'ll find faqs that have been created :'|trans() }}</p>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'ID'|trans(), 'f.id') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Question'|trans(), 'f.question') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Answer'|trans(), 'f.answer') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Is visible ?'|trans(), 'f.isVisible') }}</th>
            <th>{{ 'Actions'|trans() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td>
                    <a href="{{ path('littlebigjoe_backendbundle_faqs_show', { 'id': entity.id }) }}">#{{ entity.id }}</a>
                </td>
                <td>{{ entity.question|striptags[:80] }} ...</td>
                <td>{{ entity.answer|striptags[:80] }} ...</td>
                <td>
                    {% if entity.isVisible %}
                        <span class="label label-success">{{ 'Visible'|trans() }}</span>
                    {% else %}
                        <span class="label label-default">{{ 'Not visible'|trans() }}</span>
                    {% endif %}
                </td>
                <td>
                    <a class="btn btn-primary btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_faqs_show', { 'id': entity.id }) }}">{{ 'Show'|trans() }}</a>
                    <a class="btn btn-default btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_faqs_edit', { 'id': entity.id }) }}">{{ 'Edit'|trans() }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <p>
        <a class="btn btn-primary" href="{{ path('littlebigjoe_backendbundle_faqs_new') }}">
            {{ 'Create a new faq'|trans() }}
        </a>
    </p>
{% endblock %}
