{% extends '::admin.html.twig' %}

{% block title %}
    {{ 'New project contribution'|trans() }} | {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-header">
        <h1><span class="glyphicon glyphicon-plus"></span> {{ 'New project contribution'|trans() }}</h1>
    </div>
    <p class="lead">{{ 'Please fill these fields, to create a new project contribution :'|trans() }}</p>

    {{ form(form, {attr:{role:'form' }}) }}

    <div class="clearfix">&nbsp;</div>
    <p>
        <a class="btn btn-default" href="{{ path('littlebigjoe_backendbundle_contributions') }}">
            {{ 'Back to the list'|trans() }}
        </a>
    </p>
{% endblock %}
