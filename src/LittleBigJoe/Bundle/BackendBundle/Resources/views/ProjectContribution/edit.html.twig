{% extends '::admin.html.twig' %}

{% block title %}
    {{ 'backend.edit_project_contribution'|trans() }} #{{ entity.id }} | {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-header">
        <h1><span class="glyphicon glyphicon-edit"></span> {{ 'backend.edit_project_contribution'|trans() }} <span
                    class="text-primary">#{{ entity.id }}</span></h1>
    </div>
    <p class="lead">{{ 'backend.fill_fields'|trans() }}</p>

    {{ form(edit_form, {attr:{role:'form' }}) }}

    <div class="clearfix">&nbsp;</div>
    <p>
        <a class="btn btn-default" href="{{ path('littlebigjoe_backendbundle_contributions') }}">
            {{ 'backend.back_to_list'|trans() }}
        </a>
    </p>

    <div class="clearfix">&nbsp;</div>
    <fieldset>
        <legend>{{ 'backend.delete_project'|trans() }}</legend>
        <p>{{ 'backend.delete_confirmation_project'|trans() }}</p>

        {{ form(delete_form, {attr:{role:'form' }}) }}
    </fieldset>
    <div class="clearfix">&nbsp;</div>
{% endblock %}