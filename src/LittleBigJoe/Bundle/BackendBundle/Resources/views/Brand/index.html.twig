{% extends '::admin.html.twig' %}

{% block title %}
    {{ 'backend.brands_list'|trans() }} | {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-header">
        <h1><span class="glyphicon glyphicon-list"></span> {{ 'backend.brands_list'|trans() }}</h1>
    </div>
    <p class="lead">{{ 'backend.brands_list_resume'|trans() }}</p>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <table class="table table-striped">
        <thead>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'backend.ID'|trans(), 'b.id') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.name'|trans(), 'b.name') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.slug'|trans(), 'b.slug') }}</th>
            <th>{{ 'backend.related_projects'|trans() }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.contact_name'|trans(), 'b.contactName') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'backend.contact_email'|trans(), 'b.contactEmail') }}</th>
            <th>{{ 'backend.actions'|trans() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td>
                    <a href="{{ path('littlebigjoe_backendbundle_brands_show', { 'id': entity.id }) }}">#{{ entity.id }}</a>
                </td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.slug }}</td>
                <td>{{ entity.projects|length }}</td>
                <td>{{ entity.contactName }}</td>
                <td><a href="mailto:{{ entity.contactEmail }}">{{ entity.contactEmail }}</a></td>
                <td>
                    <a class="btn btn-primary btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_brands_show', { 'id': entity.id }) }}">{{ 'backend.show'|trans() }}</a>
                    <a class="btn btn-default btn-xs"
                       href="{{ path('littlebigjoe_backendbundle_brands_edit', { 'id': entity.id }) }}">{{ 'backend.edit'|trans() }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <p>
        <a class="btn btn-primary" href="{{ path('littlebigjoe_backendbundle_brands_new') }}">
            {{ 'backend.create_brand'|trans() }}
        </a>
    </p>
{% endblock %}
